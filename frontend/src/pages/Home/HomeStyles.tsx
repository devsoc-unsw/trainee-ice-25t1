import { styled } from "@mui/material";

const StyledH1 = styled("h1")(() => ({
    color: "#fff"
}))

const StyledDiv = styled("div")(() => ({
    padding: "2rem"
}))

export {
    StyledH1, 
    StyledDiv
}